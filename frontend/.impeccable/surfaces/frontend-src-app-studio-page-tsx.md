---
version: 1
slug: "frontend-src-app-studio-page-tsx"
primary_target: "src/app/studio/page.tsx"
related_targets: ["src/app/page.tsx", "src/app/projects/[id]/page.tsx"]
---

# Legacy demo redirect

The standalone application demo has been replaced by the owner-backed home journey. /studio redirects to /; the current home studio lives at /projects/:id and uses the corresponding saved-home brief. No session-only fixture picker, JSON editor or unsupported demo control is linked from the product.
